version: "3"

services:
  mariadb:
    container_name: mariadb
    build:
      context: ./con_volumes/mariadb
      dockerfile: Dockerfile
    restart: always
    env_file: .env
    volumes:
      - ./mount_volumes/mariadb/:/var/lib/mysql
    networks:
      - mynet
    expose:
      - "3306"
    tty: true

  wordpress:
    container_name: mywp
    build: 
      context: ./con_volumes/wordpress
      dockerfile: Dockerfile
    restart: always
    depends_on:
      - mariadb
    env_file: .env
    volumes:
      - ./mount_volumes/wordpress:/var/www/html
    networks:
      - mynet

  nginx:
    container_name: mynginx
    build: 
      context: ./con_volumes/nginx
      dockerfile: Dockerfile
    restart: always
    depends_on:
      - wordpress
    ports:
    #  - 80:80
      - 443:443
    volumes:
      - ./mount_volumes/wordpress:/var/www/html
      - ./mount_volumes/nginx/log:/var/log/nginx
    networks:
      - mynet
    expose:
     - "9000"

networks:
  mynet:
    name: mynet
