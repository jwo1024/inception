
services:
  mariadb:
    image: mariadb:10.6.4-focal
    container_name: mydb
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=mypassword
      - MYSQL_DATABASE=my_wp_db
      - MYSQL_USER=abc
      - MYSQL_PASSWORD=mypassword
    volumes:
      - ./mount_volumes/mariadb/:/var/lib/mysql
    #   - ./con_volumes/mariadb/conf.d:/etc/mysql/conf.d
    networks:
      - mynet
    expose:
      - "3306"
    tty: true

  wordpress:
    container_name: mywp
    image: wordpress:5.1.1-fpm-alpine
    restart: always
    depends_on:
      - mariadb
    environment:
      - WORDPRESS_DB_HOST=mariadb
      - WORDPRESS_DB_USER=abc
      - WORDPRESS_DB_PASSWORD=mypassword
      - WORDPRESS_DB_NAME=my_wp_db
      - WORDPRESS_TABLE_PREFIX=wp_
    volumes:
      - ./mount_volumes/wordpress:/var/www/html
    networks:
      - mynet
    tty: true

  nginx:
    container_name: mynginx
    image: nginx:1.12.2-alpine
    restart: always
    depends_on:
      - wordpress
    ports:
      - 80:80
    volumes:
      - ./mount_volumes/wordpress:/var/www/html
      - ./con_volumes/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    #  - ./nginx/a.conf:/etc/nginx/nginx.conf:ro
      - ./mount_volumes/nginx/log:/var/log/nginx
    networks:
      - mynet
    tty: true
    expose:
     - "9000"

networks:
  mynet:
    name: mynet
